!!!
%html
  %head
    %title Sign in · BackOffice · Amaferm Research

    %meta{content: "width=device-width, initial-scale=1", name: "viewport"}/
    = stylesheet_link_tag "application"
    = stylesheet_link_tag "icons", media: 'all', data: {'turbolinks-track' => true}
    = stylesheet_link_tag "external", data: { 'turbolinks-track' => true }

    / The HTML5 shim, for IE6-8 support of HTML5 elements
    /[if lt IE 9]
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
      = javascript_include_tag 'simpliq/respond.min'
      = stylesheet_link_tag 'ie6-8'

    = csrf_meta_tag
    = render partial: 'layouts/icons'
  %body#login{data: {action: params[:action], controller: controller.controller_name}}
    .container
      .row
        .page-header
          %h1.text-center
            %span.hidden-xs
              %strong> AmafermResearch
              %span BackOffice
            %span.visible-xs
              %strong AmafermResearch
              %span BackOffice

      .row
        - flash.each do |name, msg|
          .center-block.flash-container
            = render_flash name, msg
        = yield
