!!!
%html
  %head
    %title= "#{yield(:title) || "Register"} · BackOffice · Amaferm Research"

    %meta{content: "width=device-width, initial-scale=1", name: "viewport"}/
    = stylesheet_link_tag "application"
    = stylesheet_link_tag "external", data: { 'turbolinks-track' => true }

    / The HTML5 shim, for IE6-8 support of HTML5 elements
    /[if lt IE 9]
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
      = javascript_include_tag 'simpliq/respond'
      = stylesheet_link_tag 'ie6-8'

    = javascript_include_tag "application", data: { 'turbolinks-track' => true }
    = csrf_meta_tag
    = render partial: 'layouts/icons'

  %body#login{data: {action: params[:action], controller: controller.controller_name}}
    .container
      .row
        .page-header
          %h1.text-center
            %span.hidden-xs
              %strong> AmafermResearch
              %span BackOffice
            %span.visible-xs
              %strong AmafermResearch
              %span BackOffice
      .row
        .col-md-8.col-md-offset-2.content
          .row
            .col-md-10.col-md-offset-1
              - flash.each do |name, msg|
                .center-block.flash-container
                  = render_flash name, msg
              = yield
